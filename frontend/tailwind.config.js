
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // No custom colors needed as we're using direct hex values
      },
    },
  },
  plugins: [],
}